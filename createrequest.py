#The idea of this class is create responses, so the files with all ips in the set

from serves import get_servers

for server in servers["server1"]:
    print(server)

for server in serves["server2"];
    print(server)
